package org.example.elemental_power;

public interface IAgua {

    void atacarHidroBomba();
    void atracarBurbuja();
    void atacarPistolaAgua();
}

/**
 * (I) de Tipo Agua: esta será implementada por aquellas Clases que
 * determinene usar todos los métodos abstractos que esta contiene; es decir,
 * que Cada Clase que la implemente esta (I) podrá decidir como usar los métodos.
 * Clases que implementaran esta interfaz: (Squirtle)
 */
